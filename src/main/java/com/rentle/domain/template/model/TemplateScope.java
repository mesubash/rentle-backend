package com.rentle.domain.template.model;

public enum TemplateScope {
    VERIFICATION,   // provider credentials (docs/07)
    LISTING,        // extra fields collected when creating a listing
    BOOKING         // extra fields collected when requesting a booking
}
